const text = "Javacript";
const frase = 'Isso é o meu "jogo"';

let total = 0;

const compras = 30;
const imposto = 10;

total = compras + imposto;

const dobro = total * 2;
const dividir = total / 2;
const desconto = total - 30;

const strings = "20" + "20"; // Concatena os valores
const numbers = 20 + 20; // soma os valores

const transformar = Number("20") + 20; // Converte string em número

console.log(transformar);
